# =============================================================================
# BUILD - Guition ESP32-P4 JC8012P4A1 JD9365
# =============================================================================
# Thin CI wrapper: sets product name, enables AP mode for captive portal,
# and pulls the full config from the device's packages.yaml.
# =============================================================================

substitutions:
  name: "immich-frame-10-jd9365"
  friendly_name: "Immich Frame 10inch JD9365"
  espframe_component_url: "file:///config"
  espframe_component_ref: "HEAD"

esphome:
  name_add_mac_suffix: true

packages:
  espframe: !include ../devices/guition-esp32-p4-jc8012p4a1-jd9365/packages.yaml

wifi:
  ap:

captive_portal:
